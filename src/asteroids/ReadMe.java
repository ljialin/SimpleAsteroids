package asteroids;

public class ReadMe {

    // todo This code is in a state of disrepair

    // it needs fixing to ensure the gameState state can be properly
    // copied

    // a previous version worked well to play, but had a bug in the gameState state copying
    // this is now in the process of being rewritten

}
