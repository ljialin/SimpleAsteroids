package KotlinTest

import kotlin.random.*
import kotlin.math.*


fun main(args: Array<String>) {
    for (i in 0 until 10) {
// still not supported: weird
        // println(Random.nextInt(10))
    }
}